to remember stuff like configs, aliases, reasonings and my own guidelines. readme doens't need structure for now.

aliases
	yz and zd because i like two-letter commands like ls and cd and they match the names and cd≈zd

git flow 
	"since there are many ways to do it here is how i like it."

	git branch -a ... to view all branches
	git switch -c <branch_name> ... to create and switch to new branch
	git branch -d/D <branch_name> ... to delete branch

micro

yazi

gitconfig
	TODO : change to ssh key instead of https after doing ssh bandit (and learning)
